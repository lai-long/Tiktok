version: "2"

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

    - gocritic
    - lll
    - prealloc
    - exhaustive
    - nestif
    - interfacebloat
  settings:
    lll:
      line-length: 150

issues:
  max-issues-per-linter: 100
  max-same-issues: 50


